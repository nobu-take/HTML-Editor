# 引き継ぎメモ

このファイルは「次にさわる人」向けです。何がどこにあるか、なぜそう作ったか、
どこにハマりやすいかをまとめています。使い方や機能の一覧は
[README.md](README.md) を見てください。

最終更新: 2026-09-04／公開中 <https://htmleditor.karuiya.org/>

このアプリは**サーバーを持ちません**。1枚のHTMLとして動き、保存先も
メールの渡し方も、そこから決まっています。**そこが理解の要になるので、
2章から読んでください。**

直近の変更は、UIの作り直しです（5章）。窓の説明文を削り、閉じるを右上に
揃え、Gmail/Outlook のボタンを押した時点で本文をコピーするようにしました。

---

## 1. どこに何があるか

| もの | 場所 |
| --- | --- |
| 公開しているサイト | <https://htmleditor.karuiya.org/> |
| リポジトリ | <https://github.com/nobu-take/HTML-Editor> |
| 配るもの | `dist/`（**コミット済み**。ビルドしない） |
| 元 | `src/`（`Index.html` `Stylesheet.html` `JavaScript.html` `store.js` `themes.js` `Templates.js`） |
| 言語の辞書 | `locales/`（`ja.json` `en.json` と、分けて書く `parts/`） |
| マニュアル | `src/manual/ja.html` `src/manual/en.html` → `dist/manual.html` `dist/en/manual.html` |
| 道具 | `tools/`（ビルド、辞書、確認用サーバー、GIF、アイコン） |
| 権利 | [NOTICE.md](NOTICE.md) |

### ライセンスは置いていません（2026-09-03）

**再配布と改変版の公開は許諾していません。** 一度 MIT を置きましたが、
撤回しました。

公開の目的は「原稿が外に出ていないことを確かめてもらう」ことです。その確認に
必要なのは読むことと通信を見ることで、どちらも許諾は要りません。許諾が必要に
なるのは配る側になるときだけで、確認したい人は配りたい人ではありません。
MIT を選んだ理由が成り立っていませんでした。

**MIT で公開していた数時間のあいだに複製を取得した人は、その版を引き続き
MIT の条件で使えます。撤回はできません。** 公開時点でフォーク・スター・
ウォッチはいずれも0でした。

---

## 2. サーバーを持たないことから決まっていること

ここを取り違えると、直せない不具合を直そうとして時間を使います。

### 保存先はブラウザの中（localStorage）

原稿の保存先は `localStorage` です。

`src/store.js` が、保存や読み出しの関数を27個まとめて持っています。
クライアント側（`JavaScript.html`）は `call(fnName)` を呼ぶだけで、
**その先がどこに書いているかを知りません。** 保存の作りを変えるときに
さわるのは、この1か所だけで済みます。

つまり次が言えます。

- 別の端末・別のブラウザ・プライベートウィンドウからは見えない
- 閲覧データを消すと消える
- 容量に上限がある（`persist()` が `QuotaExceededError` を受けたら、
  古い版から削って入れ直す。原稿そのものは消さない）

**この制約は隠していません。** マニュアルに独立した項を立てて、消える条件を
3つ挙げ、「残したいものは書き出してください」まで書いています。

### メールの本文は、貼り付けでしか渡せない

**本文入りの下書きを自動で作ることはできません。** それには利用者本人の
権限で Gmail API を叩く必要があり、このアプリはその権限を持ちません。

道が無いわけではありません。ブラウザから Gmail API を呼べます。ただし
`gmail.compose` は Google の**制限付きスコープ**で、一般公開するには審査に
加えて第三者のセキュリティ評価が必要です（年額数千ドル規模）。趣味のアプリ
では現実的ではありません。審査を通さないと利用者100人までで、「確認されて
いないアプリ」の警告が出ます。安全性を疑われている状況では逆効果です。

他も塞がっています。

- `mailto:` と compose URL の `body` 引数は**プレーンテキストしか運べない**
- **Gmail・Outlook の Web 版には `.eml` の読み込み機能がない**

なので**貼り付けが唯一の道**です。ただしデスクトップのメールソフトなら
`.eml` で本文入りの下書きになります。「自動で入らない」のは Web 版だけです。

### ビルドコマンドを置いていない

`dist/` をコミットしてあり、Cloudflare Pages の Build command は**空**です。

- Node のバージョン差でビルドが落ちる余地を作らない
- 月500回のデプロイ枠を、別サイトと分け合っている

**`src/` を直したら、必ず `node tools/build.js` を実行して `dist/` ごと
コミットしてください。** これを忘れると、公開されているものだけ古いままに
なります。

---

## 3. 反映のしかた

```bash
cd ~/Documents/html-editor-standalone && node tools/build.js && node tools/serve.js
```

<http://localhost:8080/> で確認してから push します。**`file://` で開かないで
ください。** ブラウザによって `localStorage` の扱いが変わり、保存が黙って
消えます。

### Cloudflare の接続はリポジトリを入れ替えると切れます（2026-09-03 に踏んだ）

Cloudflare Pages は GitHub のリポジトリを **ID で追跡します。** リポジトリを
リネームすると追従し、それを削除した時点で接続が切れます。**名前が同じでも
別のリポジトリなら別物です。**

反映されていないと感じたら、まず Deployments タブで最新のデプロイの
コミットハッシュを見てください。古いところで止まっていれば接続が切れています。

**公開サイトは、存在しないURLでも index を 200 で返します。** ファイルの有無を
URL の応答で確かめてはいけません。中身を見てください。また Pages は `.html`
を落としたURLへ 308 で転送します（`/manual.html` → `/manual`）。

---

## 4. 設計上の決定と、その理由

### 「テンプレート34件 × 見た目19通り」を掛け算で作っている

一覧を646件には増やしていません。646枚を眺めるのは探しにくくなるだけです。
**先に見た目を決めて、そのうえで34件から選ぶ**ほうが早く決まります。

`src/themes.js` が5つのジャンル（ビジネス／フォーマル／カジュアル／ポップ／
ミニマル）を持ち、**色だけでなく組み方を変えます。** 見出しの扱い、ボタンの
形と幅、行間、余白、区切り線、書体、段組み、要素の並び順まで。

Beefree や Stripo のテンプレート数は、季節違いの装飾変えが大半です
（Stripo は Holidays 820 件に対し Transactional 43 件）。同じ構造を装飾で
数えるのではなく、**構造と見た目を分けて掛け合わせる**ほうが少ない手数で
広がります。

#### 変換の順番が意味を持ちます（4回踏みました）

`applyTheme()` は次の順に適用します。

```
色 → 文字の大きさ → 見出しの装飾 → 見出しの太さ・字間 → 余白の密度
→ 角丸 → 罫線 → 影 → 明朝への差し替え → ボタンの余白・角丸
→ ボタンの幅 → 段組み → 並び替え → 帯のあとの余白
```

**目印を消してから、その目印を探す**という順番にすると壊れます。実際に
起きたことです。

- ボタンの余白 `16px 44px` を、あとの処理が `21px 57px` に膨らませた
- 角丸 `999px` が `2248px` になった
- 見出しの装飾が、太さを normal にしたあとに走り、フォーマルに効かなかった
- ボタンの幅が `display:block` を先に付け、余白の判定が空振りした

順番を変えるときは、`node tools/diff-genres.js` で19通り×44項目を突き合わせて
ください。td/tr/table の数、属性の区切り、余白の値、白文字の contrast（4.5 以上）
を見ます。

### 多言語は「決まった場所での完全一致の置き換え」

`tools/build.js` が、次の場所だけを置き換えます。

- タグの内側の文字
- `placeholder` `title` `aria-label` `alt`
- JavaScript の文字列リテラル
- テンプレート内の HTML コメント

**素朴な文字列置換にしていないのは、「保存」が「保存先」を食うからです。**
辞書の鍵は**日本語の原文そのもの**なので、訳し漏れは画面に日本語のまま出ます。
黙って空欄になりません。

#### ハマりどころが4つあります

**① 鍵は原文なので、改行が変わると別の文字列になります。**
編集に使った道具が改行を CRLF に書き換え、複数行の訳が7件外れました。
**`src/` を書き換える道具は、改行を LF で保つものを使ってください。**

**② 英語のアポストロフィが JS を壊します。**
`'The AI's reply'` で白画面になりました。`escapeFor()` で処理しますが、
**訳が当たったときだけ**エスケープします。全部に適用すると、日本語側の
`"'Hiragino Sans','Yu Gothic'"` や `'​'` が壊れます（これも踏みました）。

**③ 抽出の出力先は `locales/_source.json` です。**
一度 `ja.json` を上書きして、日本語版の文言が作りかけのものに戻りました。
画面には出てしまうまで気づけません。

**④ 2文をつなげて1つの案内にしないでください。**
日本語は文と文のあいだに空白が要りません。分けて連結すると、その形のまま
英語にも出て `(Ctrl + V).If you have not` と詰まります。**区切りは言語ごとに
違うので、言語ごとに全文を持たせてください。**

作業の順番は次のとおりです。

```bash
cd ~/Documents/html-editor-standalone && node tools/extract-strings.js && node tools/merge-locale.js en && node tools/merge-locale.js ja && node tools/build.js
```

`merge-locale.js` は「元の文言に無い鍵」を報告します。**打ち間違いは黙って
無視されるので、この報告は必ず読んでください。**

### クリップボードは同期の道を先に試す

`copyRich()` が `copy` イベントで `setData('text/html')` と
`setData('text/plain')` を明示的に載せ、`preventDefault()` します。
きっかけは同期の `document.execCommand('copy')` です。

**選択範囲をブラウザに勝手に整形させると、空になります。** 隠した要素
（`left:-9999px; opacity:0`）に選択を置く作り方で、実際に空の下書きに
なりました。

同期が通らなかったときだけ `navigator.clipboard.write()` を試します。
こちらは終わるのが利用者が別のタブへ移ったあとになるので、案内は届きません。
それでもクリップボードには入るので貼り付けは通ります。

---

## 5. UI で決めたこと（2026-09-04）

説明文を削る作業をしました。**基準は次の2つで、どちらも満たさない文は
マニュアルへ移しました。**

1. 読んだ結果、次にやることが変わる
2. 読んだ結果、不安が消える

| 窓 | 説明文 |
| --- | --- |
| メールにする | 205文字 → 66文字 |
| AIに聞く | 363文字 → 201文字 |

### 画面の作りを言葉で書き写していた

両方とも、**並びがすでに示していることを隣に書いていました。**

AI の窓は「左で決める → 右上を渡す → 右下に戻す」という3段に並んでいます。
そこへ「1. 依頼を選ぶ / 2. コピーしてAIへ / 3. 回答を取り込む」という一覧を
別に置いていました。**一覧を消して、番号を段の見出しへ移しました。** 言葉は
減りますが、番号と場所が結びつくので、むしろ探しやすくなります。

メールの窓の3手順も同じです。しかもボタンを直したあとは**記述が事実と
違っていました**（押した時点でコピーするので「1. コピー」「2. 開く」は
分かれていない）。

### 行き先で束ねる

Gmail と Outlook は「開いてから貼り付ける」という**同じ一文を共有します。**
手順ごとに書くと3か所に散りますが、まとまりの見出しの下に置けば1回で済みます。

### 警告は、関係する操作のそばに置く

AI の窓には110文字の枠付きの警告を常設していました。**常にあるものは
読まれません。** 2つに分けて、それぞれの隣に置きました。

- チェックの下 → 「含めると、本文のHTMLがそのまま文面に入ります」（何が起きるか）
- コピーの下 → 「個人名やアドレスが入っていないか、コピーする前にご確認ください」（何を確かめるか）

### 閉じるは右上の ✕、問いかける窓は下に「キャンセル」

`✕` は「この窓を閉じる」という意味を、**OSの窓の右上にあるという位置から
得ている記号**です。下の操作列に置くと手がかりが消え、記号として何も
伝えません。位置ごと移してください。

分け方は役目で決めています。

- **並べる・見せる窓**（テンプレート／AIに聞く／Webページ／署名／版の履歴／
  保存場所／依頼の登録／メールにする）は右上に `✕`。下の「閉じる」は置かない
- **問いかける窓**（削除の確認）はそのまま。あちらの「キャンセル」は窓を
  閉じる意味ではなく「その操作をしない」という**答え**なので、もう一方の
  選択肢と並んで下にあるのが正しい

結果として、下に残るのは**その窓でしかできないこと**だけになりました。

### 全体に効かせるものは、場所を数えない

スクロールバーの見た目を、はじめは使う場所ごとに指定していました。**数え漏らした
ところに既定の明るいつまみが出て、暗い画面の中で白く浮きます。** 実際、署名・
テンプレート・部品ツール・本体編集画面・コードが漏れていました。

いまは `*` と `::-webkit-scrollbar` で全体に当てています。**数え漏らしが原因の
不具合は、数えない形にしないと同じことが起きます。**

例外は白い地の上だけ（`.sig-preview`）。暗い溝を敷くと、白い紙の上に黒い帯が
乗ります。

### コード画面の色

`--code-*` のトークンにまとめてあります。**すべて地 `#14171c` の上で
contrast 4.5 以上**（4.52〜14.07）。コードは全部が本文なので、読みにくい色を
1つも置きません。色を足すときは `scratchpad` の測り方と同じ式で確かめてください。

**セレクタは class 2つで書くこと。** CodeMirror の既定テーマは
`.cm-s-default .cm-tag` と書かれているので、`.cm-tag` だけでは負けます。
読み込み順はこちらが後なので、同じ強さにすれば勝ちます。1つで書いて色が
変わらず、詰まりました。

### 面の有無で優先度を表さない

メールの窓の行き先4つは同じ面を持たせています。Gmail/Outlook だけに面を
付けたら、**地のままの2つが「押せない文字」に見えました。**

### 変えたいものだけを見せる（署名の編集）

署名はHTMLをそのまま書かせていました。ですが署名で変えたいのは、文面と、
文字の大きさ・行間・色くらいです。**そのためにタグを書かせる理由がありません。**
本文はただの複数行の入力欄にし、見た目は選ぶ形にして、結果をプレビューで
見せています。

プレビューを白地にしてあるのは、**原稿が白い紙だから**です。暗い地の上では
色の出方が変わります。

#### 読み戻せないものを、黙って作り変えないこと

これまでに登録した署名はHTMLで入っています。**読み戻せない形を組み立てで
扱おうとすると、書いたものが失われます。**

`signatureParts()` は、組み立てたときと同じ形（`div` ひとつに `<br>` で
区切った文字だけ）のときだけ読み戻し、それ以外は `null` を返します。
`null` のときはHTMLのまま編集させ、その旨を画面に出します。表・画像・
リンク・`div` が2つ・空は、すべて `null` です。

組み立てと読み戻しの往復は、空行・タグに見える文字・記号 `& < > "` を
含めて文章が変わらないことを確認してあります。**ここを変えるときは、
必ず往復を確かめてください。**

一度、往復で改行が倍になりました。組み立てたHTMLは読みやすさのために行ごとに
改行を入れてあり、`<br>` を改行に置き換えると、そのあとの文字ノードが持つ
改行と字下げも数えていました。**`<br>` で切って、切れ端ごとに文字を取ります。**

### 数が増えても位置が動かないようにする

AI の依頼の種類は、横に流して折り返す形をやめました。登録した依頼が増える
ほど背が高くなり、下の入力欄を押し下げていたからです。**縦のリストにして
高さを固定（143px）し、入りきらないぶんはスクロールさせます。**

高さは**行がちょうど半分で切れるところ**に合わせてあります。つまみの見え方は
環境で変わる（オーバーレイ式だと触るまで出ない）ので、切れた行で続きがある
ことを示します。

---

### 文字にリンクを付ける（2026-09-05）

既存の `<a>` は要素パネルで直せましたが、**新しく作る道がありませんでした。**
道具の帯に、色の欄と同じ「入力＋適用」の並びで足しています。

ふだんは絵のボタンだけを出し、押したときに欄が開きます。道具の帯は横に
長いので、使わないものを常に置くとほかの操作が押し出されます。

決めたことが3つあります。

- **`example.com` と書かれたら `https://` を補います。** そのまま `href` に
  入れると相対パスと解釈され、メールでは開けません（`mailto:` `tel:` は
  そのまま通します）
- **`target` と `rel` は付けません。** メールのHTMLでは意味を持たず、
  受け取った側のソフトが決めます。付けると、落とすソフトと落とさない
  ソフトで見た目が割れます
- **外すときは `<a>` を中身で置き換えます。** 文字は残ります

`surroundContents` は選択が要素の境界をまたぐと落ちるので、装飾を当てる
ときと同じ逃がし方（中身を取り出して包み直す）をしています。

---

## 6. 直した不具合と、その原因（同じ形で再発します）

### `[hidden]` が効かないボタンがあった

`button { display: inline-flex }` が、ブラウザ既定の
`[hidden] { display: none }` **より強い**ため、`hidden` を付けたボタンが
そのまま出ていました。`?`（マニュアル）が「見えているのに押しても何も
起きない」状態だったのはこれです。

`button[hidden] { display: none; }` を入れて直しました。**全体に `[hidden]`
を `!important` で効かせていないのは、`.panes.split .pane[hidden]` のように
わざと表示している箇所があるためです。**

### ダイアログの操作ボタンが画面外に出て押せなかった

`.dialog` は `max-height: 100%` で枠を縮めますが、**中身は縮みません**
（flex の子は既定で内容より小さくならない）。溢れたぶんは下へ押し出され、
`margin-top: auto` で最下部にある `.dialog-actions` が画面の外に出ます。

**縦768pxのノートPCでも、ブラウザのUIを引くと表示領域は600px台になります。**
珍しい条件ではありません。画面高560pxで再現しました。

`overflow-y: auto` でダイアログの中をスクロールさせ、操作列を `sticky` で
常に見えるようにしました。**スクロールできること自体に気づかず「壊れている」
と受け取られる**ので、sticky は必須です。

「Gmail に貼り付かない」という報告の原因は、貼り付け処理ではなく**これ**
でした。ボタンを押せていませんでした。

### 貼り付けの順番を、動作の前提にしていた

「コピー」と「開く」を別の操作にしていました。順番を守れば動きますが、
**守らなかったときに何も起きません。** 押した本人には、貼り付けても空のまま
に見えます。

**Gmail/Outlook のボタンを押した時点で、本文のコピーも済ませるようにしました。**
コピーは同じクリックの中で行うので、ブラウザから利用者の操作として扱われます。

### 貼り付けたいところに余白の値を直接書いていた

`.dialog-head` の負マージンを `-22px` と書いていましたが、`.ai-card` の余白は
20px でした。2px 突き出して**横スクロールバーが出ました。**

余白は `--pad-x` / `--pad-y` で持ち、入れ物が自分の値を申告します。

### 空になった行がコードに残っていた

面を持つ枠（色の帯など）は `td` の単位で選ばれます。消すと `<tr></tr>` だけが
残り、**プレビューには出ないのにコードには残っていました。**

いまの条件は **「枠がひとつも残っていないときだけ片付ける」** です。
`pruneEmptyRow()` は `node.cells.length` を見るだけで、それ以外は何もしません。

**この条件に落ち着くまで、2回広げすぎました。記録として残します。**

1回目は「空になったら消す」。署名を消すと、その上の区切り線ごと無くなりました。

2回目は「背景も罫線も無ければ消す」。署名として挿し込む枠は `padding` しか
持たないので、中の部品を消すと枠ごと消えました。

```html
<tr><td style="padding:8px 32px 24px 32px;"><div>署名</div></td></tr>
```

**枠が残っているなら消さない、が正解でした。** 空でも余白として場所を占め、
選ぶこともできるので、見えているものと書き出されるものの食い違いは起きて
いません。**消すよう頼まれたのは中身であって、枠ではありません。** 空いた枠に
別のものを入れる使い方もあります。

つまり、直す対象は最初から**「枠ごと無くなって `<tr></tr>` だけが残る」場合
だけ**でした。それ以外に広げるたびに、頼まれていないものを消していました。

`td` を単体で消さないのは、2カラムの片方だけが無くなると残った側の幅が
変わるからです。

### 表示サイズを変えると、コードの選択に色が付ききらなかった

アプリ全体の表示サイズは、根の要素の `zoom` で当てています。**CodeMirror は
選択の色を「行ごとの矩形」として重ねて描きますが、その高さを測るときに
`zoom` を考えていません。** 倍率のぶんだけ矩形が短くなります。

```
倍率0.85:  色 862px / 中身 1021px = 0.84   下の16%に色が付かない
```

**選択の範囲そのものは正しい**ので、コピーは全文できます。だから
「見えている範囲とコピーされる範囲が食い違う」という形でしか出ません。
`editor.refresh()` を呼んでも直りません。測り方そのものが合っていないためです。

コード欄の中だけ `zoom` を打ち消しています。`zoom` は掛け算で伝わるので、
`1 / var(--ui-scale)` を当てれば CodeMirror から見た倍率が 1 になります。
それだけだと文字が元の大きさに戻るので、`font-size` に倍率を掛けて見た目を
保っています。

**`--ui-scale` は `setUiScale()` と起動時の2か所で入れています。片方だけ
直すと、そのときだけ食い違います。** 表示サイズまわりをさわるときは両方を
見てください。

なお、この不具合は**両方のリポジトリにありました**。片方でだけ報告されたのは、
保存されている表示サイズが別々だったからです。**片方で表示サイズがらみの
不具合が出たら、もう一方も同じ倍率で確かめてください。**

### 入力欄の中で Ctrl+Z が効かなかった

画面側の `keydown` が Ctrl+Z を横取りし、本文側の取り消しを実行していました。
除外していたのは件名欄だけで、署名の編集などダイアログの入力欄でも横取りして
いました。`preventDefault()` するので、ブラウザ本来の取り消し・やり直しも
動きません。

`keysAreOurs()` が「入力欄の中か、ダイアログが開いているなら受け取らない」を
判定します。**キーを足すときは必ずこれを通してください。** Delete 側には同じ
判定が最初からあり、Ctrl+Z だけが漏れていました。

### 英単語が途中で割れていた

`.note` の `word-break: break-all` が `Copy the bo dy` にしていました。
長いURLのための指定でしたが、**必要なときだけ割る `overflow-wrap: anywhere`**
にすべきでした。日本語は既定でどこでも折り返せます。

---

## 6.5 スマートフォン対応（2026-09-05）

スマホから触れるようにしました。**ここに書いてあることは、どれも実機で測って
初めて分かったものです。** Chrome の模擬では最後まで再現しませんでした。

### プレビューが指にまったく反応しなかった

原因は `sandbox` の指定ひとつでした。

```
Index.html   <iframe id="preview" sandbox="allow-same-origin">
```

**iOS/iPadOS の WebKit は、`allow-scripts` の無い枠にイベントを1つも配りません。**
`touchstart` すら来ません。プレビューは編集面そのものなので、指では何にも
触れない状態でした。

実機に確認用のページを置いて、枠の作り方を5通り並べて測りました。

| 枠の作り方 | 届いたイベント |
|---|---|
| `sandbox="allow-same-origin"` + srcdoc（当時） | **0件** |
| `allow-scripts` を足す | 42件 |
| sandbox なし | 16件 |
| sandbox なし + `document.write` | 31件 |

**`srcdoc` も sandbox 自体も関係なく、足りないのは `allow-scripts` だけ**でした。

#### どこで緩めるか

`allow-same-origin` と `allow-scripts` を併せると sandbox は防御になりません
（枠の中から自分で外せます）。だから**起きる場所だけ**に絞ります。

**これは WebKit だけの挙動です。** Chrome や Edge では同じ指定でもイベントが
届きます。なので「指かどうか」ではなく「iOS/iPadOS かどうか」で見分けます
（`wantsTouchPreview()`）。タッチ画面の Windows 機も Android も緩めません。

`pointer: coarse` は使いませんでした。**読み込みの初期に答えが定まらず、
外した回はずっと強いままになります**（計測で8回中4回外しました）。
UA と指の点の数は最初から確定しています。

判定は**描画のたびに引き直します**。起動時に一度だけだと、外した回が固定されます。

#### 掃除を足しました

描画経路には掃除が無く、コード画面に書いた `<script>` がそのまま
プレビューに入っていました。これまでは sandbox が止めていただけです。

メールにスクリプトは入りません。GmailもOutlookも必ず落とすので、
**落としたほうがプレビューは実物に近くなります。**

**ただし消しません。** プレビューでの編集は `serializeWys()` を通って本文へ
書き戻るので、**消すと利用者の書いたものが黙って失われます。**
`calmForPreview()` が型と属性名を変えて眠らせ、`wakeAfterPreview()` が
書き戻しのときに起こします。

`<script>` も `on*` も無い原稿には**何もしません**（整形すら通しません）。
テンプレート34種はどれも該当しないので、ふつうの原稿では描画経路が
1バイトも変わりません。

### 表示サイズが、原稿を左へ寄せていた

`html` の `zoom` が原因です。**Safari は `zoom` を掛けても画面の幅を
取り直しません。** 中身だけが縮むので、0.85なら右に15%の余りが残ります。
Chrome は幅も取り直すため、模擬では再現しません。

狭い画面では当てないようにし、入口（表示サイズ）も畳みました。
**その場で押せてしまえば同じことが起きる**ためです。覚えた値は消しません。

判定に `screen.width` を使っています。**この処理は `viewport` の指定より前に
走る**ので、`matchMedia` はまだ980px扱いで答えます（一度これで外しました）。

### 帯に `overflow` を使ってはいけません

**ここは2回壊しました。** ヘッダーとタブの帯は4つのメニューを抱えています。

| メニュー | 場所 |
|---|---|
| 取り出す | ヘッダー |
| この原稿の扱い | ヘッダー |
| 表示サイズ | ヘッダー |
| 紙面の幅 | タブの帯 |

**`overflow` を指定した箱は、中から飛び出す要素を切り取ります。**
メニューは下に開くので、全部切られます。折り返し（`flex-wrap`）なら
切られませんが、ヘッダーが63px→273pxになり、見る場所を奪います。

道具の帯（`.tool-row`）だけはメニューを持たないので、流してかまいません。

### メニューが画面の外に出ていました

`.pop` は「ボタンの右端にそろえて幅300px」です。画面の左寄りにある
ボタンから開くと、**左が画面の外へ出ます**（390pxで最大98px）。

狭い画面では下から出す形にしました。ボタンがどこにあっても必ず収まります。

**この見落としの原因は検査にあります。** 前回の事故が `overflow` による
切り取りだったので、「祖先による切り取り」だけを見る検査になっていて、
**画面の外へ出ているかを見ていませんでした。** いまは四方すべてを測ります。

### 画面の下は、下から順に積んでいます

```
部品の帯（position: fixed, bottom: 0）   いちばん下
ファイル一覧 / 要素パネル のボタン        その上（bottom: 46px）
要素パネル                                さらに上（bottom: 92px）
```

部品の帯を固定にしたぶん流れから外れるので、`.canvas-col` に
`padding-bottom` を入れて原稿が裏へ潜らないようにしています。
**最初 `.editor-body` に付けて効きませんでした。測ったら `0 / 0` で、
高さを持たない入れ物でした。**

### メールへの書き出しは、スマホでは開きません

`?view=cm` はPC版Gmailの新規作成URLで、**スマホでは受信トレイへ転送されます**
（実機で確認）。開いた先が新規作成でないなら、こちらで開く意味がありません。

押して確実に起きるのはコピーだけなので、それを主役にして、開くのは
踏めるリンクとして渡します。自分で踏んだリンクなら iOS に止められません。

なお **Gmailのアプリに貼ると背景色が落ちます**（アプリ側の貼り付けの掃除に
よるもので、こちらからは変えられません）。ブラウザ版なら残るので、
案内に一言添えてあります。

### 窓を狭めたときの畳み方

ヘッダーの中身は1196pxで縮み止まります。それ以下では右から順に画面外へ
出ていました。幅に応じて、名前が無くても分かるものから畳みます。

| 幅 | 畳むもの |
|---|---|
| 1240px | 新規・テンプレート・AI・取り出す・1画面に収める・全画面 を絵だけに |
| 1040px | 「for Email」、言語欄の余白 |
| 1000px | 紙面の幅の表示 |
| 940px | 状態表示 |
| 840px | 名札 |
| 760px | 折り返す＋メニューを画面の右下へ |

---

## 7. 検証

### 道具

| もの | すること |
| --- | --- |
| `tools/check-api.js` | クライアントが呼ぶ27個が `store.js` に揃っているか |
| `tools/diff-genres.js` | 19通りの配色を突き合わせる（構造の壊れ、contrast） |
| `tools/preview-genres.js` | ジャンルごとの見本を書き出す |
| `tools/preview-accents.js` | 差し色ごとの見本を書き出す |
| `tools/merge-locale.js` | 訳し漏れと、元に無い鍵を報告する |
| `tools/make-gif.js` | 紹介用のGIF（Chromeを動かして撮る） |
| `tools/make-icons.js` | タブのアイコン |

### プレビューの操作には自動テストがありません

削除・移動・複製・取り消しは、**手で確かめてください。** 2026-09-04 に
4テンプレート×4操作＝16通りを、次の観点で確認しました。

- 枠を持たない `<tr>` が残らないか
- 空の行・空の枠が残らないか
- `td` の総数 = 各行の枠の合計（つじつま）
- HTML を読み直して要素数が変わらないか（入れ子の壊れ）
- **取り消しで、元の文字列にそのまま戻るか**

最後がいちばん効きます。16通りすべてで一字も違わず戻りました。2カラムの
テンプレートと、`td 11 > tr 9` の複数枠を持つテンプレートを必ず含めてください。

### iOS の挙動は、手元では再現できません

**Chrome の模擬（Blink）と iOS Safari（WebKit）は別物です。** このセッションで
起きた不具合は、どれも模擬では最後まで再現しませんでした。

- `sandbox` にイベントを配るかどうか
- `zoom` を掛けたときに画面の幅を取り直すかどうか

**模擬で動いたことを、実機で動く証拠として扱わないでください。**

確かめたいことがあるときは、`dist/` に確認用の1枚を置いて実機で開いて
もらい、数値を返してもらうのが速いです（役目が終わったら消します）。

### レイアウトの検査は、四方すべてを測ってください

祖先による切り取りだけを見ると、**画面の外へ出ているものを見逃します。**
実際に見逃してメニューが操作できない状態を出しました。

```
left < 0 / right > 画面幅 / top < 0 / bottom > 画面高
＋ 祖先の overflow による切り取り
```

### 書き換えのたびに、構文と括弧を検査してください

このセッションで3回、**書き換えそのものが壊れました。**

| 壊れ方 | 表に出た形 |
|---|---|
| `'\n'` が実際の改行になり文字列が閉じない | アプリ全体が動かない |
| 正規表現の `\s` が消える | 判定が静かに効かない |
| `@media` が途中で閉じる | スマホ用の指定がPCへ漏れる |
| コメントが二重になる | そこから先の指定が丸ごと無効 |

いずれも `node -e` やヒアドキュメント経由でバックスラッシュが失われた
ことが原因です。**引用符やバックスラッシュを含む編集は、スクリプトファイル
を経由してください。** そのうえで、

- JS: `new Function(コード)` で構文を通す
- CSS: 括弧の釣り合い、`/*` と `*/` の数、`@media` の入れ子

を毎回確認します。

### 見た目の確認は、画面が低い状態で

**縦560pxを標準の確認条件にしてください。** ここで起きる不具合を、縦900pxでは
見つけられません。

---

## 8. 残っていること

- **対応言語は日本語と英語だけ**です。`locales/index.json` に言語を足し、
  `parts/` に訳を書けば増やせます。Karuiya 本体は8か国語です
- **`.eml` を実際のメールソフトで開く確認**をしていません
- **サイトは誰でも開けます**（Cloudflare Access を掛けていない）
- **Karuiya 側に英語の入口がありません**
- Outlook のURLが `outlook.live.com`（個人向け）です。**職場・学校の
  アカウントでは開かない可能性があります**
- 紹介用のGIFは `assets/demo/`（英語UI・日本語UIの2本）。Xへの投稿文は
  作成済みですが、リポジトリには入れていません
- **部品の縦帯（`.tool-rail`）は、狭い画面では横に寝かせています。**
  絶対配置で幅44px固定の作りなので、位置・幅・向きをまとめて上書きして
  います。触るときは3つとも見てください（向きだけ変えると幅0になります）
- **スプレッドシートのグラフは `data:` の画像として入ります。** 画像そのものが
  原稿に埋まるので相手には見えますが、**1枚で数十〜数百KB**になります。
  保存は localStorage の5MBが上限なので、何枚も入れると埋まります
- **要素パネルの幅は52%（208px）**です。面積で測ると半分以下でも、
  **目に入るのは左右の占め方**です。詰めるときは幅の割合で見てください

---

## 9. 見落としやすいところ

次の5つは、**どれも「見えているものと、実際の動きが食い違う」**たちのもの
です。画面を見ているだけでは気づけないので、さわったら意識して確かめて
ください。詳しくは6章に書いてあります。

- `button { display: inline-flex }` と `[hidden]` の関係
- `.dialog` の `max-height` と溢れ
- `.dialog-actions` の折り返し
- `removeSelected()` の空になった枠の扱い
- 表示サイズの `zoom` と、コード欄の選択の色
