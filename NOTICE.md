# 権利について

## このアプリのコード

`src/` と `tools/` のコードは、すべて自作です。著作権は作者が保持します。

**ソースは公開していますが、再配布と、改変版の公開は許諾していません。**

読むこと、動きを確かめること、手元で動かしてみることは自由です。
公開している理由がそれだからです。このアプリはサーバーを持たないので、
原稿がどこにも送られていないことは、開発者ツールのネットワークを見れば
その場で確認できます。コードを読めば、さらに確かめられます。

配布を許諾していないのは、中身を落とした複製が「同じもの」として
出回ることを避けたいからです。使いたい方は、公開しているサイトを
そのままお使いください。

- <https://htmleditor.karuiya.org/>

別の使い方をしたい場合は、ご相談ください。

## 外部のライブラリ

### CodeMirror 5.65.16（MIT ライセンス）

コード画面のエディタに使っています。

**同梱していません。** cdnjs から読み込むだけです。

```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.16/codemirror.min.js"></script>
```

同梱していないので再配布にはあたりませんが、MIT ライセンスであることを
ここに記しておきます。

- <https://codemirror.net/5/>
- Copyright (C) by Marijn Haverbeke and others / MIT License

**同梱する形に変えるとき**（オフラインで使いたい等）は、
ライセンス条文の写しを一緒に置いてください。MIT はそれを求めています。

## 画像

`assets/img/` の見本画像7枚は、`tools/make-images.js` が
グラデーションと図形だけで描いたものです。**外部の素材は使っていません。**

PNGの書き出しは、Node の `zlib` だけを使って自前で行っています
（仕様は公開されているもの）。

## アイコン

画面のアイコンは、すべて 16×16 で自作した SVG です。
アイコンフォントやアイコンセットは使っていません。

## フォント

テンプレートの中でフォント名を指定していますが、**名前を書いているだけ**で、
フォントファイルは同梱していません。

## テンプレートの文言

`src/Templates.js` のテンプレート34件は、この配布物のために書いたものです。
用途に合わせて自由に書き換えてください。
